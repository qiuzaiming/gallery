<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LightHouseGallery" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">#FFFFFF</item>
        <!-- Customize your theme here. -->
        <item name="fontFamily">@font/lxgwwenkai_regular</item>

        <item name="preferenceTheme">@style/ThemeOverlay.Preference</item>
    </style>


    <!--custom view add style-->
    <style name="Theme.Style.Loading.TextView" parent="Theme.LightHouseGallery">
        <item name="android:textViewStyle">@style/loading_textView</item>
    </style>

    <style name="loading_textView">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="ThemeOverlay.Preference" parent="PreferenceThemeOverlay">
        <item name="android:textSize">13sp</item>
        <item name="android:paddingVertical" tools:ignore="NewApi">0dp</item>
    </style>

</resources>